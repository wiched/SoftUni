function projectsCreation(name, projects) {
  const hours = projects * 3;
  console.log(`The architect ${name} will need ${hours} hours to complete ${projects} project/s.`);
}

projectsCreation('George', 4);
